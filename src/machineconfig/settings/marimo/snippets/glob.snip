# Title: Globally run a function (glob)
# Trigger: glob
# Description: Exec run of func globally via lambda_to_python_script
from machineconfig.utils.meta import lambda_to_python_script
exec(
    lambda_to_python_script(
        lambda: ▶func◀(),
        in_global=True,
        import_module=False
    ),
)